<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="LacunaMatata.Lacuna.repository.user.UserRoleMetMapper">
    <insert id="saveUserRoleMat" useGeneratedKeys="true" keyProperty="userRoleMetId">
        insert into tb_user_role_met
        values(
            default,
            #{roleUserId},
            default,
            now(),
            now()
        )
    </insert>
</mapper>